const SearchInfo = () => {
    // Info like search time and if spelling was correct etc.

    return (
        <>
            <p>{}</p>
            <p>{}</p>
        </>
    )
}

export default SearchInfo;